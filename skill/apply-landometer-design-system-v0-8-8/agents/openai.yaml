interface:
  display_name: "Landometer DS v0.8.8"
  short_description: "Apply the approved v0.8.8 authoring master"
  default_prompt: "Use $apply-landometer-design-system-v0-8-8 to apply or audit this artifact against the owner-approved authoring master and report every open machine and artifact gate."
policy:
  allow_implicit_invocation: false
