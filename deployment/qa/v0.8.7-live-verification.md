# v0.8.7 live verification trigger

This source-limited QA record triggers the GitHub-hosted verifier for the deployed Landometer Design System v0.8.7 HTML and Manifest 2.0.

The associated pull-request check is authoritative for the live HTTP result. It verifies the repository release first, then fetches the GitHub Pages endpoint with retry and requires:

- HTML `data-ds-version="0.8.7"`;
- HTML `data-manifest-version="2.0"`;
- the v0.8.7 Living Reference title;
- deployed Manifest version `2.0` and Token Schema `6`;
- `machineValidation: pending` and `indexable: false`.

This record does not close the separately listed Thai, recognition, identity-surface, media/privacy, or screen-reader manual gates.
