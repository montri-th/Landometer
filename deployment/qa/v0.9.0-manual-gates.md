# Landometer Design Identity Playground v0.9.0 — open manual gates

This source-limited internal team playground is prepared from the owner-approved v0.9.0-r1 authoring master, approved on 20 August 2026. Authoring authority and the exact atmosphere recipes are approved, but UI build `ui-20260820-01` is not yet published and the regenerated public-safe machine package remains pending. It does not claim package-level or artifact-level conformance. The following gates remain open, so the page stays `internal_demo`, `source_limited`, `noindex`, and `machineValidation: pending`.

1. Thai read-aloud naturalness, Thai–English governed-value parity, and cross-device confirmation that continuous-reading Thai uses self-hosted Bai Jamjuree while technical Thai glyphs use self-hosted IBM Plex Sans Thai 400 beside JetBrains Mono 400. Confirm the technical pair renders only at actual weight 400 with no active JetBrains Mono 500/700 or IBM Plex Sans Thai 500 request, embedded standalone payload, or faux bold. Inspect combining marks, clipping, mixed-script baselines, compact-pill height, `.008em` tracking, the 102% face adjustment, readability at 12–16 CSS px, 130% Thai text, and 200% zoom; no operating-system Thai fallback may appear.
2. Five-reviewer Landometer recognition plus the 5-second invitation, 30-second AHA, and 2-minute takeaway checks.
3. Identity-neutralized substitution, brand deletion, generic-language deletion, decoration deletion, photo substitution, and gradient deletion reviews.
4. Exact logo variant, delivered size, clear space, first-paint behavior, and surface-pairing approval; logo and release metadata must stay transparent with no box, plate, carrier, border, or radius in every state. Verify the approved browser-tab-only symbol at `assets/images/landometer-symbol-transparent.png`: exact 192 × 192 RGBA PNG, 11,001 bytes, recorded SHA-256, transparent canvas/alpha bounds, unchanged light/dark/system delivery, no crop or carrier, and no reuse as social/touch/maskable/header identity. Hosted HTML must request `assets/images/landometer-symbol-transparent.png?v=35a1496f`; standalone HTML must request the same bytes through `https://montri-th.github.io/Landometer/assets/images/landometer-symbol-transparent.png?v=35a1496f`; neither may use a `data:` favicon. Complete small-size tab, bookmark, shortcut, reload, cache-replacement, supported-theme, and hostname-level search-favicon review.
5. Exact team-photo owner, creator credit, context/date, rights, expiry, privacy, visible-screen, and public-rendition approval.
6. Perceptual-quiet region measurement and human attention review at the required desktop and mobile viewports.
7. Review `qa/v0.9.0-gradient-contrast.json`: both declared inks must pass 4.5:1 at every one of 1,001 sRGB samples for all seven shared atmosphere gradients. Then complete the remaining rendered-glyph review for the shared header surface, transparent logo/release metadata, Measure hero, Brand DNA Measure preset, muted Brand Voice Ground preset, selectable Brand Visual atmospheres, closing surface, focus rings, separators, translucent metadata panels, and control boundaries.
8. Responsive inspection at 320, 360, 390, 768, 1024, 1180, and 1440 CSS pixels; Thai at 130%; all content at 200% zoom.
9. Dataviz light/dark, color-vision-deficiency, grayscale, 200% zoom, table parity, direct-label, unit/source/period, measured-zero/no-data, missing-period, and counter-signal review for `DV-FIXTURE-0.1` plus snapshot/source and treatment-sketch separation for `CITYMETER-DV-SNAPSHOT-2026-07-29`.
10. `COLOR-ATLAS-0.9.0` review in light and dark themes at 320 and 390 CSS pixels: verify the first-class complete atlas remains preserved and findable; the concise sampler shows all 6 sequential + 3 diverging families and exact 5/7/9 classes (189 paired-theme cells); the disclosure retains 17 foundation pairs, 7 semantic states, 7 shared atmosphere gradients, 3 asset-only motif records (including one alias diagram), 4 product-gradient families/8 theme specimens, 10 categorical pairs, 18 analytical scale records/738 LUT cells/378 class cells, 8 map pairs, 8 opacity values, and 6 depth roles. Repeat under representative color-vision-deficiency simulations and grayscale; verify that shape, pattern, labels, measured zero, and no-data remain distinct.
11. Color-boundary review: verify all 15 v0.8.9 gradient-only sRGB values remain `gradient_stop_only`; Measure/Ground/Cultivate use their frozen light/dark defaults; Diversity appears only in a truthful multiple-perspective or co-creation scene and never more than once per long route; `motif.gradient.brandSignature` is an exact alias rather than a duplicated palette. Analytical scales remain visibly `source_limited` because `scales.json` carries `meta.version: 0.8.6`; do not treat it as the gated v0.9.0 `dataviz.tokens.json`. Confirm that product gradients appear only as product-identity specimens and are never used for shared atmosphere, data, maps, interaction, or semantic states.
12. Standalone-file review: first open `deployment/index.html` through `file://` in Safari and confirm it hands off to `landometer-design-system-v0.9.0-standalone.html` without losing query parameters or the fragment and without issuing a local cross-origin font preload. Then open the standalone directly with network access blocked; confirm all nine WOFF2 faces and all page-content images render, representative Thai 400/600/700 and governed Latin companion glyph loads return loaded faces, `data-thai-body-ready`, `data-thai-technical-ready`, `data-thai-display-ready`, `data-latin-companions-ready`, and `data-fonts-ready` are `true`, and `data-font-delivery` transitions from `pending` to `ready` without reaching `failed`, `timeout`, or `unavailable`. Confirm theme/language/disclosure controls work, the complete color atlas matches the hosted page, no display-critical relative asset request is made, and the portable snapshot does not claim the hosted canonical URL. Repeat in supported Safari, Chrome, and Firefox builds. The browser-tab symbol is the declared exception: it may be absent while offline because its stable production URL is retained for cross-browser favicon consistency; reconnect, reload, and confirm that the exact cache-revisioned approved symbol appears without changing any page meaning.
13. Implementation Library findability, native disclosure keyboard behavior, seven-group coverage including the first-class complete color route, product-adaptation boundary, and downloadable-master byte/hash review. Confirm text-labelled and mixed icon-plus-label actions use `radius-pill`, wrap without clipping, and stack at narrow widths; icon-only theme/language utilities remain 44 × 44 circles; cards, fields, disclosures, tabs, and segmented selectors retain their own geometry.
14. Full keyboard route, quiet theme cycle (`system → light → dark`), language cycle (`TH ↔ EN`), clipboard failure recovery, and supported desktop/mobile screen-reader matrix.
15. Regenerated v0.9.0 public-safe machine package, release lock, source fingerprint, rule ledger, schemas, recipes, and preflight.
16. `UX-FIXTURE-0.1` review: three positive Intent → AHA cases, one rejected engagement-first case, eight foundation opportunities, four component ensembles, and three dataviz extensions are varied, relevant, findable, and clearly non-operational where required.
17. `MOTION-FIXTURE-0.1` review: Replay, Pause/show final, visible event log, 120/200/280/360/400/560 ms roles, no autoplay, no-JavaScript final meaning, and reduced-motion immediate final meaning across supported browsers and assistive technology.
18. `CONTEXT-FIXTURE-0.1` review: four current CityMETER record sources, retrieval date, missing provenance, omission of values with unresolved scope, runtime-derived event/date/cause, public-safe active field label, place hierarchy, one visible/editable `q`, two explicit Google result routes, new-tab preservation, privacy removal, cancellation/failure recovery, and `discovery_only` result treatment. Any relevance claim requires a same-intent baseline-versus-assisted study.

    - **Ordinary Search versus AI Mode:** activate each submit button separately and inspect the outgoing GET before any redirect. Both routes must send the exact currently visible `q`; ordinary Search must omit `udm`, while AI Mode alone must add `udm=50`. There must be no hidden prompt, hidden query field, background request, automatic opening, or automatic fallback.
    - **Availability and locale:** test Thai and English UI in each supported browser and region with signed-out, signed-in eligible, and signed-in ineligible Google accounts. Record whether AI Mode is available, unavailable, relabelled, redirected, or handled differently. The Landometer page must not claim universal availability or alter the ordinary Search route when AI Mode is unavailable.
    - **Account and personalization boundary:** repeat with Google personalization/history settings on and off where testers can do so safely. Record visible differences without treating personalized output, summaries, ranking, or account state as deterministic Landometer behavior or evidence.
    - **Sent-query visibility:** before activation, verify the full query remains readable, editable, selectable, and associated with both buttons. After activation, verify the submitted request contains no value beyond the visible `q` plus the activated AI Mode submitter's `udm=50`; do not enter private identity, private filters, notes, customer criteria, restricted IDs, or sensitive locations.
    - **Failure and recovery:** test pop-up blocking, offline mode, unavailable AI Mode, sign-in requests, empty/invalid query, Google error/redirect, and back navigation. The current page and edited query must remain intact; ordinary Search must remain usable; retry, edit, copy, cancel, or close must be possible without a duplicate or automatic request.
    - **Source verification:** treat Search and AI Mode output as `discovery_only`. Open and inspect cited/original sources, compare date/place/event/field scope with the visible CityMETER source record, reject unsupported synthesis or mismatched entities, and deliberately incorporate only reviewed material. AI-generated wording, citation presence, or confident tone must never upgrade provenance.
    - **Mobile, keyboard, and assistive technology:** at 320 and 390 CSS pixels and 200% zoom, confirm the input and both submit buttons remain distinct, readable, and fully visible. Traverse label → query → ordinary Search → AI Mode with keyboard and supported screen readers; verify distinct accessible names, focus visibility, Enter/Space behavior, new-tab announcement, and a reliable route back to the unchanged fixture.
19. `NETWORK-FIXTURE-0.1` review: the recipient-value ladder remains static, no live share/invite/send control appears, no delivery or network outcome is simulated, and any future share requires product-owned recipient, permission, visibility, redaction, receipt, and recovery. Govern telemetry only if separately enabled; require observed governed outcome evidence before claiming network effect.
20. SEO/machine-delivery review: HTML remains `noindex` while the artifact has internal-team intent through a public projection and does not claim delivery conformance. Verify that machine-readable-when-opened, search-discoverable, AI-search-discoverable, and agent-readable/actionable remain separate recorded states; Open Graph/JSON-LD/sitemap stay absent; linked public-safe artifacts are recognized as outside the HTML meta directive; `robots.txt` remains explicitly non-authoritative at the project path; the exact approved transparent browser-tab symbol is rendered without promoting it to social/touch/maskable/header duty; the standalone stays noncanonical; and `llms.txt` remains an optional emerging-convention navigation aid with no agent permission.
21. Two-device Color Set/build-parity gate, performed in this order:

    - **Prove delivery parity first:** on both devices, record the Design System version, `data-color-registry`, `data-artifact-build`, `data-build-channel`, token-registry path/version/SHA-256, scale-registry path/version/SHA-256, and the expected SHA-256 for each channel under review. Confirm that the hosted root/latest alias, latest standalone alias, current immutable UI artifact, Complete Color Atlas, Site Manifest, and Build Card all bind to registry `color-srgb-03` and artifact build `ui-20260820-01`. For each selected artifact, both devices must receive the same expected bytes/hash. Confirm that latest and immutable UI standalones are equivalent after normalizing only `data-build-channel`.
    - **Use the immutable authority:** before publication, conduct current UI approval from local file `deployment/landometer-design-system-v0.9.0-standalone.color-srgb-03.ui-20260820-01.html`. After publication and exact-byte verification, hand off from `https://montri-th.github.io/Landometer/landometer-design-system-v0.9.0-standalone.color-srgb-03.ui-20260820-01.html`. The earlier `...standalone.color-srgb-03.html` remains the immutable Color Set baseline and must retain its recorded bytes/hash; it is not rewritten for UI-only changes. Treat `index.html` and `landometer-design-system-v0.9.0-standalone.html` as latest aliases. Because GitHub Pages may serve those aliases with `Cache-Control: max-age=600`, a temporary difference is cache/build skew; stop, wait or purge/reload as governed, and re-prove parity rather than describing it as color drift.
    - **Then compare rendering:** after registry, build, and hashes match, pin both devices to the same explicit `light` theme and repeat with `dark`; never use `system` as the comparison baseline. Record device, operating system, browser/version, display, HDR/SDR state, and color-management/profile context. Compare representative foundation/semantic colors, all six Measure/Ground/Cultivate tonal recipes, the rare Diversity Spectrum, categorical cues, and exact LUT/class cells. Confirm the declared SDR sRGB values and explicit sRGB interpolation path where supported. Treat explicit sRGB as secondary renderer stabilization only; any remaining difference after parity/theme proof is a renderer/display investigation, not permission to alter the registry.
22. Localized scale-label collision gate: inspect the concise scale sampler and Complete Color Atlas at 320, 360, 390, 620, 621, 980, and 981 CSS pixels in EN and TH, Light and Dark. For every 5/7/9-class row, measure the rendered text range—not only the figcaption element box. When label and strip share a line, require the text’s right edge to remain at least 8 CSS pixels before the strip. When space is insufficient, require the intact strip to wrap below the intact label with at least 8 CSS pixels of block gap. Confirm no horizontal scrolling, clipping, label fragmentation, text painted beneath color, or strip collapse below 8rem; preserve every 5/7/9 equal-width cell and capsule geometry. Verify that each sampler accessible name follows the active locale and resolved theme only, while Atlas names identify their fixed record theme. Repeat representative cases at 200% text zoom and with the supported browser/font matrix.

23. Purple/brown purge gate `[COLOR-01]`: search the hosted page, all three standalone artifacts, `assets/data/tokens.json`, `assets/data/scales.json`, and `assets/data/color-delivery.v0.9.0.json` for each retired value — `#795300`, `#846100`, `#686354`, `#8B877A`, `#B6AD98`, `#A59A80`, `#9E476F`, `#E982AE`, `#827C68`, `#85837A`. Every occurrence must be inside an immutable `color-srgb-01`/`color-srgb-02` evidence record, never in a live v0.9.0 token, scale LUT, class cell, gradient stop, or rendered surface. Confirm `data-color-registry="color-srgb-03"` on every channel and confirm each replacement keeps its role contrast: `#A64A00`, `#A87B00`, `#5C6A61`, `#7B877D`, `#A7B3A9`, `#93A398`, `#B23F74`, `#F06FA6`, `#7C8A84`, `#8D9D99`.
24. Icon lock gate `[ICON-01]`: every icon glyph must come from the embedded Material Symbols Rounded subset at `FILL 0, wght 300, GRAD 0, opsz 24`. Assert zero `<svg><use>` sprite references, zero inline decorative SVG icons, and zero second icon family. Verify the outline stroke reads thin and the corners read rounded at 16, 20, and 24 CSS pixels in both themes, that every icon-only control keeps its accessible name, and that the subset renders with network access blocked in the standalone artifacts.
25. Two-shape action geometry gate `[BTN-GEOM-01]`: enumerate every activatable control. Each must resolve to exactly one of two shapes — a capsule (`radius-pill`) for text or icon-plus-label actions, or a 44 × 44 circle for icon-only utilities. Any third radius on an action is a failure. Re-check after wrapping at 320 and 390 CSS pixels and at 200% text zoom.
26. Riddim motion gate `[MOTION-01]`: confirm the named signature uses the unchanged v0.8.9 durations — 60 ms skank stagger for lists, one-drop settle for results, 120 ms downbeat press — and only the two new easing tokens. Verify `prefers-reduced-motion: reduce` lands the final meaning immediately with no stagger, and that no motion is required to read any governed value.
27. Media-ladder and first-view disclosure gate `[MEDIA-LADDER-01]` + `[DISCLOSURE-01]`: in the human-facing first view of every route, assert that no caveat, caution, limitation, or `source_limited` prose is visible before interaction. Each such statement must live behind an explicit disclosure the reader opens. Confirm the ladder order actually used — video, image, icon or symbol, then text — and that the collapsed state still answers the reader's question without the caveat.
28. Edge-rail prohibition gate `[VIS-04]`: assert there is no decorative vertical edge rail, bracket spine, or ornamental frame border on any section, card, or panel. Structure must be carried by the governed surface, spacing, and type scale instead. Inspect both themes at 320, 768, and 1440 CSS pixels.
29. Release-receipt, per-page favicon, and share-preview gate `[PUB-01]`: confirm the in-page release receipt reports the same Design System version, Color Set, and artifact build as `data-*` identity and the Site Manifest; confirm the browser-tab identity matches the page content; and test the share preview on LINE, Facebook, and LinkedIn against the real destination URL rather than a local preview.
30. `[SELFCHECK-01]` gate — **record shipped, two rows open**: the nineteen-row binary self-check was run on 2026-08-20 with a deterministic DOM/text checker and its result rows ship in `site-manifest.v0.9.0.json` (`selfCheck`), matching the rendered table on the page. Verdicts: 15 pass · SC-12/SC-13 n/a (internal noindex page, no promoted share destination) · SC-02/SC-03 **fail with recorded `[EXCEPTION-01]`** because this artifact is a pre-Kit build upgraded in place — the carried v0.8.9 theme-init and stylesheet are still the styling source instead of the Appendix E kit bytes. The open work is kit consolidation for this page; new artifacts must copy the kit verbatim and re-run the self-check at every release.

These open gates do not authorize product, city, dataset, customer, municipality, retail, F&B, CityWiki, CityMETER, CityChat, or ijji claims. Shared Landometer guidance remains product-neutral; product truth remains product-specific.
