# Landometer Design Identity Playground v0.8.8 — open manual gates

This source-limited internal team playground is published from a public repository and built from the owner-approved v0.8.8 authoring master. It does not claim package-level or artifact-level conformance. The following gates remain open, so the page stays `internal_demo`, `source_limited`, `noindex`, and `machineValidation: pending`.

1. Thai read-aloud naturalness, Thai–English governed-value parity, and cross-device confirmation that human-readable compact Thai labels load self-hosted Bai Jamjuree rather than an operating-system fallback.
2. Five-reviewer Landometer recognition plus the 5-second invitation, 30-second AHA, and 2-minute takeaway checks.
3. Identity-neutralized substitution, brand deletion, generic-language deletion, decoration deletion, photo substitution, and gradient deletion reviews.
4. Exact logo variant, delivered size, clear space, first-paint behavior, and surface-pairing approval; logo and release metadata must stay transparent with no box, plate, carrier, border, or radius in every state.
5. Exact team-photo owner, creator credit, context/date, rights, expiry, privacy, visible-screen, and public-rendition approval.
6. Perceptual-quiet region measurement and human attention review at the required desktop and mobile viewports.
7. Worst rendered-sample contrast review for the shared header surface, transparent logo/release metadata, Measure hero, selectable atmosphere fixture, closing surface, focus rings, and control boundaries.
8. Responsive inspection at 320, 360, 390, 768, 1024, 1180, and 1440 CSS pixels; Thai at 130%; all content at 200% zoom.
9. Dataviz light/dark, color-vision-deficiency, grayscale, 200% zoom, table parity, direct-label, unit/source/period, measured-zero/no-data, missing-period, and counter-signal review for `DV-FIXTURE-0.1` plus snapshot/source and treatment-sketch separation for `CITYMETER-DV-SNAPSHOT-2026-07-29`.
10. `COLOR-ATLAS-0.8.8` review in light and dark themes at 320 and 390 CSS pixels: verify 17 foundation pairs, 7 semantic states, 2 shared gradients, 3 motif gradients, 4 product-gradient families/8 theme specimens, 10 categorical pairs, 18 analytical scale records/738 LUT cells/378 class cells, 8 map pairs, 8 opacity values, and 6 depth roles. Repeat under representative color-vision-deficiency simulations and grayscale; verify that shape, pattern, labels, measured zero, and no-data remain distinct.
11. Color-boundary review: analytical scales remain visibly `source_limited` because `scales.json` carries `meta.version: 0.8.6`; do not treat it as the gated v0.8.8 `dataviz.tokens.json`. Confirm that product gradients appear only as product-identity specimens and are never used for shared atmosphere, data, maps, interaction, or semantic states.
12. Standalone-file review: open `landometer-design-system-v0.8.8-standalone.html` directly from the local filesystem with network access blocked; confirm embedded fonts and images render, theme/language/disclosure controls work, the complete color atlas matches the hosted page, and no display-critical relative asset request is made.
13. Implementation Library findability, native disclosure keyboard behavior, six-group coverage, product-adaptation boundary, and downloadable-master byte/hash review.
14. Full keyboard route, quiet theme cycle (`system → light → dark`), language cycle (`TH ↔ EN`), clipboard failure recovery, and supported desktop/mobile screen-reader matrix.
15. Regenerated v0.8.8 public-safe machine package, release lock, source fingerprint, rule ledger, schemas, recipes, and preflight.
16. `UX-FIXTURE-0.1` review: three positive Intent → AHA cases, one rejected engagement-first case, eight foundation opportunities, four component ensembles, and three dataviz extensions are varied, relevant, findable, and clearly non-operational where required.
17. `MOTION-FIXTURE-0.1` review: Replay, Pause/show final, visible event log, 120/200/280/360/400/560 ms roles, no autoplay, no-JavaScript final meaning, and reduced-motion immediate final meaning across supported browsers and assistive technology.
18. `CONTEXT-FIXTURE-0.1` review: four current CityMETER record sources, retrieval date, missing provenance, omission of values with unresolved scope, runtime-derived event/date/cause, public-safe active field label, place hierarchy, one visible/editable `q`, two explicit Google result routes, new-tab preservation, privacy removal, cancellation/failure recovery, and `discovery_only` result treatment. Any relevance claim requires a same-intent baseline-versus-assisted study.

    - **Ordinary Search versus AI Mode:** activate each submit button separately and inspect the outgoing GET before any redirect. Both routes must send the exact currently visible `q`; ordinary Search must omit `udm`, while AI Mode alone must add `udm=50`. There must be no hidden prompt, hidden query field, background request, automatic opening, or automatic fallback.
    - **Availability and locale:** test Thai and English UI in each supported browser and region with signed-out, signed-in eligible, and signed-in ineligible Google accounts. Record whether AI Mode is available, unavailable, relabelled, redirected, or handled differently. The Landometer page must not claim universal availability or alter the ordinary Search route when AI Mode is unavailable.
    - **Account and personalization boundary:** repeat with Google personalization/history settings on and off where testers can do so safely. Record visible differences without treating personalized output, summaries, ranking, or account state as deterministic Landometer behavior or evidence.
    - **Sent-query visibility:** before activation, verify the full query remains readable, editable, selectable, and associated with both buttons. After activation, verify the submitted request contains no value beyond the visible `q` plus the activated AI Mode submitter's `udm=50`; do not enter private identity, private filters, notes, customer criteria, restricted IDs, or sensitive locations.
    - **Failure and recovery:** test pop-up blocking, offline mode, unavailable AI Mode, sign-in requests, empty/invalid query, Google error/redirect, and back navigation. The current page and edited query must remain intact; ordinary Search must remain usable; retry, edit, copy, cancel, or close must be possible without a duplicate or automatic request.
    - **Source verification:** treat Search and AI Mode output as `discovery_only`. Open and inspect cited/original sources, compare date/place/event/field scope with the visible CityMETER source record, reject unsupported synthesis or mismatched entities, and deliberately incorporate only reviewed material. AI-generated wording, citation presence, or confident tone must never upgrade provenance.
    - **Mobile, keyboard, and assistive technology:** at 320 and 390 CSS pixels and 200% zoom, confirm the input and both submit buttons remain distinct, readable, and fully visible. Traverse label → query → ordinary Search → AI Mode with keyboard and supported screen readers; verify distinct accessible names, focus visibility, Enter/Space behavior, new-tab announcement, and a reliable route back to the unchanged fixture.
19. `NETWORK-FIXTURE-0.1` review: the recipient-value ladder remains static, no live share/invite/send control appears, no delivery or network outcome is simulated, and any future share requires product-owned recipient, permission, visibility, redaction, receipt, and recovery. Govern telemetry only if separately enabled; require observed governed outcome evidence before claiming network effect.
20. SEO/machine-delivery review: HTML remains `noindex` while the artifact has internal-team intent through a public projection and does not claim delivery conformance; Open Graph/JSON-LD/sitemap stay absent, linked public-safe artifacts are recognized as outside the HTML meta directive, `robots.txt` remains explicitly non-authoritative at the project path, and `llms.txt` remains an optional navigation aid with no agent permission.

These open gates do not authorize product, city, dataset, customer, municipality, retail, F&B, CityWiki, CityMETER, CityChat, or ijji claims. Shared Landometer guidance remains product-neutral; product truth remains product-specific.
